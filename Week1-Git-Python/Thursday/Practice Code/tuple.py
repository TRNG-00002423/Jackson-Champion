#Tuple: ordered, immutable (cannot be changed)

empty_tuple = ()

rgb_color = (255,255,0)

coordinates = (10,20)


def results(marks):
    # based on marks calculate the percentage and returns the percentage
    # based on percentage calculated return the grade
    percentage = 85
    grade = "B"
    return (percentage.grade)    